/**
 * Global audio constants used across the app.
 */
export const LOUDNESS_TARGET_LUFS = -14;         // Integrated loudness target
export const TRUE_PEAK_CEILING_DBTP = -1;        // Max true‑peak allowed (dBTP)

/**
 * Default container / codec when probe() fails.
 */
export const FALLBACK_FORMAT = 'wav';
